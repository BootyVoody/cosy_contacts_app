import 'package:flutter/material.dart';

class ShortenedContactCard extends StatelessWidget {
  const ShortenedContactCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => Container();
}
